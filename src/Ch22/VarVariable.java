package Ch22;
// Java 1.8 unable to run
import java.util.Arrays;
import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

public class VarVariable {

	public static void main(String[] args) {
//		var list = Arrays.asList("Apple", "Banana", "Cherry", "Fig");
//		var filterList = new ArrayList<String>();
//		for (var s : list) {
//			if(s.length() > 5) {
//				filterList.add(s);
//			}
//		}
//		System.out.println(filterList);
//		// 用list中的stream()方法配合filter(lambda).collect(Collectors.toList())轉成一個list
//		var filterList = list.stream().filter(s -> s.length() > 5).collect(Collectors.toList());
//		System.out.println(filterList);
//		//用list中的stream()方法配合map(用在每個元素根據提供的函數逕行轉換)(lambda).collect(Collectors.toList())將字串轉成一個list
//		var filterList2 = list.stream().map(s ->s.toUpperCase()).collect(Collectors.toList());
//		System.out.println(filterList2);
		
		
		
	}

}
